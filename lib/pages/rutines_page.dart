import 'package:flutter/material.dart';

class RutinePage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('rutines'),
      ),
    );
  }
}